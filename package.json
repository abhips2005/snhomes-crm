{
  "name": "snh-crm",
  "private": true,
  "version": "0.1.0",
  "description": "S N Homes Kerala real estate matchmaking PWA",
  "scripts": {
    "dev": "corepack pnpm --filter @snh/web dev",
    "build": "corepack pnpm --filter @snh/types build && corepack pnpm --filter @snh/web build",
    "lint": "corepack pnpm --filter @snh/web lint",
    "typecheck": "corepack pnpm --filter @snh/types typecheck && corepack pnpm --filter @snh/web typecheck"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.11.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
